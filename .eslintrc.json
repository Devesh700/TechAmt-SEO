{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "overrides": [
    {
      "files": ["src/pages/api/**/*.ts"], // Target API files
      "rules": {
        "@typescript-eslint/no-explicit-any": "off" // Disable the rule only in these files
      }
    }
  ]
}
